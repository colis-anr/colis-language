(include_subdirs unqualified)

(ocamllex colisLexer)
(menhir (flags --explain) (modules colisParser))

(executable
 (name concrete_colis)
 (public_name colis)
 (libraries zarith str)
 (flags :standard -w -27))
