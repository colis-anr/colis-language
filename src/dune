(include_subdirs unqualified)

(ocamllex colisLexer)
(menhir (flags --explain) (modules colisParser))

(executable
 (name colis)
 (public_name colis)
 (libraries why3.extract str ppx_deriving_yojson.runtime morsmall)
 (flags :standard -w -27))
