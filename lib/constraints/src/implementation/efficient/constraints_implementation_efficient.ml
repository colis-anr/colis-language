include Constraints_implementation_efficient_clause.External
(* include Constraints_implementation_efficient_clause.PrettyPrinter *)
let pp _ = assert false
let pp_as_dot ~name _ = ignore name; assert false
let make_initial _ = assert false

let kind k x = kind x k
let nkind k x = nkind x k
